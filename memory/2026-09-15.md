# 2026-09-15

## Workspace Sync Policy
- **저장소 원칙**: `iiac-BAT` 프로젝트(`c:\Users\moonh\Documents\iiac-BAT`)는 **GitHub 원격 저장소(`https://github.com/moonhac72/iiac-BAT.git`)에만 동기화**되며, **옵시디언(Obsidian Sync 등 옵시디언 클라우드/메인 지식창고)에는 절대 동기화하지 않음**.
- 로컬 내의 Obsidian 앱으로 마크다운을 볼 수는 있으나, 옵시디언 싱크(`core-plugins.json`의 `"sync": false`)는 비활성화 상태 유지 및 모든 백업/동기화는 Git / GitHub 푸시로만 진행함.
- **동기화 트리거**: 사용자가 `/깃허브에 저장해줘` 입력 시 자동으로 `git status` -> `git add .` -> `git commit -m "..."` -> `git push origin <branch>` 파이프라인을 실행하여 GitHub에 업로드 동기화함.

